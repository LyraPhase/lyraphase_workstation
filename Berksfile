#source 'http://api.berkshelf.com'
source "https://supermarket.chef.io"

group :integration do
  cookbook "minitest-handler"
end

cookbook 'sprout-base', '~> 0.1.0', :github => 'pivotal-sprout/sprout-base'

metadata
