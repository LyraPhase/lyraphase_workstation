# /etc/auto_nfs - NFS mounts on OS X
# Generated by Chef for <%= node['fqdn'] %>
#
# For mounting things under /Volumes you must use a tricky hack:
#    https://discussions.apple.com/message/17561102#17561102
#    https://gist.github.com/L422Y/8697518
#
# We simply use the '..' up-a-directory idiom to go up a path, then select /Volumes
# For example:
#/../Volumes/foo    -fstype=nfs,noowners,nolockd,noresvport,hard,bg,intr,rw,tcp,nfc nfs://192.168.1.100:/export
<% @nfs_mounts.each do |nfs_mount_line| -%>
<%= nfs_mount_line %>
<% end -%>
